RSpec.describe ServerspecExtensionTypes do
  it 'has version' do
    expect(ServerspecExtensionTypes::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
