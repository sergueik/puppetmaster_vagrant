database_name = "<%= @admin_database -%>"

db_handle = db.getSiblingDb(database_name)

db_handle.createUser({
  user: "<%= @admin_user -%>",
  pwd: "<%= @admin_password -%>",
  roles: [
    { role: "readWrite", db: database_name }
    { role: "dbowner", db: database_name }
  ]
  })

